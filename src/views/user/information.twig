<@{{ memberId }}> ({{ memberName }}) has been a member for {{ tenureDays }} of the {{ requiredTenureDays }} required days.
They have posted {{ totalComments }} of the {{ requiredCommentCount }} comments required.
{% if isLevelOne %}
    They have been given {{ levelOneRoleName }}.
{% else %}
    They have not been given {{ levelOneRoleName }}.
{% endif %}
{% if isVerified %}
    They are {{ verifiedRoleName }}.
{% else %}
    They are not {{ verifiedRoleName }}.
{% endif %}
{% if isEligible %}
    They are eligible for automatic assignment to {{ levelOneRoleName }}.
{% else %}
    They are not eligible for automatic assignment to {{ levelOneRoleName }}.
{% endif %}
{% if isVerified and verifiedById %}
    Verified By: <@{{ verifiedById }}> ({{ verifiedByName }})
{% endif %}