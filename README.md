# SpudBot
A Discord bot based on DiscordPHP.

## Configuration

Running `bin/setup.php` will automatically handle these steps for you.

```shell
php bin/setup.php
```

1. Install composer packages, ```composer install ```
2. Copy env config, ```cp .env.example .env```
